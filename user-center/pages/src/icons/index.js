/**
 * Barrel export for project-local, Lucide-compatible custom icons.
 *
 * Import icons the same way you would `lucide-react`:
 *   import {ShieldOAuth} from '@/icons';
 *
 * Add a new icon by dropping a file in this directory that exports
 * a `createIcon(name, paths)` result, then re-export it from here.
 * Keep the file/name kebab-case and the export PascalCase so React
 * devtools + eslint no-unused-vars conventions stay consistent with
 * lucide-react.
 */
export {createIcon} from './create-icon';
export {ShieldOAuth} from './shield-oauth';
